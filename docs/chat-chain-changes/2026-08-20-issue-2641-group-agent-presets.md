---
date: 2026-08-20
pr: 2644
feature: Group Agent presets
impact: Existing Room Agent editing opens a dedicated searchable management dialog for non-secret preset CRUD, while adding an Agent to an existing Room uses a separate searchable chooser that copies only after explicit Apply. Cancel leaves the form unchanged, unavailable presets stay visible with their exact validation reason, Room creation does not expose presets, and later preset edits never rewrite existing Room Agents.
---
